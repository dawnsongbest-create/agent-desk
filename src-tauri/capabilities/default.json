{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Least-privilege capabilities for the bundled Agent Desk main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
